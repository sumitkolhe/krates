import { RequestHandler } from 'express'
import { Logger } from '@src/utils/logger'

export const getBucketData: RequestHandler = async () => {
  try {
    // ok
  } catch (error) {
    Logger.error(error)
  }
}

export const setBucketData: RequestHandler = async () => {
  try {
    // ok
  } catch (error) {
    Logger.error(error)
  }
}
